# Staged at build, never committed: the bundled transcription engine.
# CI stages per platform (see the Build App workflow); locally, build
# whisper.cpp v1.8.2 with BUILD_SHARED_LIBS=OFF and copy whisper-cli here.
